//package com.mycompany.bookshop.util;



import java.util.ArrayList;
import java.util.List;

//public class Querybuilder {
 /*   public static List<String> buildAllQueries(final  List<Book> books) {
        ArrayList<String> queries = new ArrayList<String>();

        for (Book book : books) {
            queries.add(Querybuilder.buildQuery(book));
        }

    return  queries;
    }

    public static String buildQuery(Book book) {
        String query = "INSERT INTO books (isbn, title, author, editionYear, price) VALUES ('";
        String comma = "', '";
        String finalChars = "');";
        StringBuilder st = new StringBuilder();
        st.append(query);
        st.append(book.getIsbn() + comma);
        st.append(book.getTitle() + comma);
        st.append(book.getAuthor() + comma);
        st.append(book.getEditionYear() + comma);
        st.append(book.getPrice() + finalChars);
        String result = st.toString();
        return  result;
    }*/

//}
